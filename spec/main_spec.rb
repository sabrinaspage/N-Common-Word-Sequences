require 'main'

describe 'MostCommonWordSequences' do
  describe "call" do
    it "returns hash which meets challenge criteria" do

    end
  end
  describe "strip" do
    it "strips the file" do

    end
  end
  describe "sequence_has_empty_strings" do
    context "has empty strings" do
      it "returns true" do

      end
    end
    context "does not have empty strings" do
      it "returns false" do

      end
    end
  end
  describe "word_seq" do
    context "array has only empty strings" do
      it "returns empty" do

      end
    end
    context "array has one empty string" do
      it "returns string with all strings" do

      end
    end
    context "array has strings" do
      it "returns string with all strings" do

      end
    end
  end
  describe "word_sequence_frequencies" do
    it "returns the correct frequencies based on the input" do

    end
  end
  describe "descend" do
    it "descends the hash by value" do

    end
  end
  describe "top_common_word_sequences" do
    it "returns the top #{MostCommonWordSequences.NUM_OF_SEQUENCES} values in the hash" do

    end
  end
end